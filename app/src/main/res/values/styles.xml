<resources>

    <!-- Base application theme. -->
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/dark_gray</item>
        <item name="colorPrimaryDark">@color/dark_gray_status</item>
        <item name="colorAccent">@color/gray</item>
        <item name="android:colorControlNormal">@color/gray</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/gray</item>

    </style>
    <style name="AppAuth" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/dark_gray</item>
        <item name="colorPrimaryDark">@color/dark_gray_status</item>
        <item name="colorAccent">@color/gray</item>
        <item name="android:colorControlNormal">@color/gray</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/gray</item>
    </style>

    <style name="RegistrationEditTextStyle">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textColorHint">@color/dark_gray</item>
        <item name="android:textColor">@color/gray</item>
        <item name="android:tint">@color/gray</item>
    </style>

    <color name="white">#ffffff</color>
    <color name="gray">#c4c4c4</color>
    <color name="dark_gray">#5db6ab</color>
    <color name="dark_gray_status">#22858f</color>
    <color name="black">#101010</color>
</resources>
